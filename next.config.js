/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration normale pour le web (Vercel)
  // Pas de 'output: export' ici pour garder les API routes !
}

module.exports = nextConfig
